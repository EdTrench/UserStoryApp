@model UserStoryApp.Models.Story

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Story</legend>

        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.Children)
        @Html.HiddenFor(model => model.Parent.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Narrative)
        </div>
        <div class="editor-field-narrative">
            @Html.EditorFor(model => model.Narrative)
            @Html.ValidationMessageFor(model => model.Narrative)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Priority)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Priority)
            @Html.ValidationMessageFor(model => model.Priority)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Estimate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Estimate)
            @Html.ValidationMessageFor(model => model.Estimate)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
